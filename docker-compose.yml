version: '3'
services:
  web:
    build: api
    ports:
      - "8080:8080"
    links:
      - "db"
    volumes:
      - "./api:/go/src"
  db:
    image: postgres:latest
    ports: 
      - "5432:5432"
    environment:
      - "POSTGRES_USER=postgres"
      - "POSTGRES_PASSWORD=mysecretpassword"