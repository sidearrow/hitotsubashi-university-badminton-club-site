{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "set-dev": "firebase functions:config:set env.env=\"dev\"",
    "set-prod": "firebase functions:config:set env.env=\"prod\"",
    "serve": "npm run set-dev && firebase functions:config:get > .runtimeconfig.json && firebase serve --only functions",
    "deploy-dev": "npm run set-dev && firebase use dev && firebase deploy --only functions",
    "deploy-prod": "npm run set-prod && firebase use default && firebase firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "jest",
    "test-all": "nyc mocha test/index.test.js 2>&1 | tee test.log"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "firebase-admin": "^6.5.0",
    "firebase-functions": "^2.1.0"
  },
  "private": true,
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-http": "^4.2.0",
    "jest": "^24.1.0",
    "mocha": "^5.2.0",
    "nyc": "^13.0.1",
    "supertest": "^3.4.2"
  },
  "engines": {
    "node": "8"
  }
}
